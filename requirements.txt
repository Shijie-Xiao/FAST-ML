# FAST_ML inference dependencies.
# Pinned to lower bounds that are known to work; newer releases are expected to
# be fine. The published results were produced with torch 2.6.0+cu124,
# numpy 2.4.6, xarray 2026.2.0 and netCDF4 1.7.4.
torch>=2.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
xarray>=2023.1
netCDF4>=1.6
matplotlib>=3.7
einops>=0.6

# Optional: only needed by scripts/download_data.py for Google Drive hosted files.
gdown>=4.7
# Optional: only needed by scripts/plot_strike_probability.py (strike maps).
cartopy>=0.21
